function soma (a, b) {
      return a + b
    }
    let resultado = soma(8, 20)
    console.log(resultado)

    //nome da função:soma
    //parametros: a, b
    //palavra reservada = resultado